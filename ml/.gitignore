# Regenerable / heavy — never commit
.venv/
models/
__pycache__/
gen/logs/
gen/__pycache__/
standing-loop/*.log
standing-loop/state.json
standing-loop/retrain-*.log

# Real-speech + derived training data — LOCAL ONLY, never public
data/
