{
  "name": "beers-pubwall",
  "version": "1.0.0",
  "private": true,
  "description": "Beers Pub Wall API — opt-in leaderboard that never receives transcripts or audio; see README/schema for its full metadata boundary.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute beers-pubwall --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260819.1",
    "typescript": "5.9.3",
    "wrangler": "^4.124.0"
  }
}
